<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ============================================= -->
    <!-- 🔹 Main App Theme (Material3) -->
    <!-- ============================================= -->
    <style name="Theme.SmartAssist"
        parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Transparent system bars -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

    </style>


    <!-- ============================================= -->
    <!-- 🔹 True Transparent Theme (NO UI FLASH) -->
    <!-- Used ONLY for ScreenCapturePermissionActivity -->
    <!-- ============================================= -->
    <style name="Theme.SmartAssist.Transparent"
        parent="@android:style/Theme.Translucent.NoTitleBar">

        <!-- Fully transparent window -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">false</item>

        <!-- No title / no action bar -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>

        <!-- 🔥 Remove activity enter/exit animations -->
        <item name="android:windowAnimationStyle">@null</item>

    </style>

</resources>